= Default postgress server info =

database: postgres
user: postgres
host: localhost
pw: user password 
<!-- if using posgres user and pw is not set, u can do it with $ALTER USER postgres WITH PASSWORD 'new_password'; from postgres cli -->